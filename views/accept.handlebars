<!--Navigation Bar-->
  <nav class="navbar  navbar-inverse navbar-fixed-top">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
     </div>
     <div class="collapse navbar-collapse" id="navbar-collapse">
         <ul class="nav navbar-nav">
              <li><a href="/">HOME</a></li>
              <li><a href="/api/accept">ACCEPT</a></li>
              <li><a href="/assignJobs">ASSIGN</a></li>
              <li><a href="/viewJobs">VIEW</a></li>
              <li><a href="/reports">REPORTS</a></li>
         </ul>
        <ul class="nav navbar-nav navbar-right admin-nav">
              <button type="submit" class="btn btn-primary" id="adminLogin" data-toggle="modal" data-delay = "50" data-target="/">Log Out<i class="fa fa-key" aria-hidden="true"></i></button>
            </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav> <!--End Nav-->

<!--Webpage Body-->
<div class="container" style="position: relative; top: 45px;">
  {{#each Job}}
  <div class="panel panel-default job-panel">
    <div class="panel-body">
      <div class="row accept-row">
        <div class="col-xs-6">
          <p><strong>NAME:</strong> {{this.client_name}}</p>
          <p><strong>LOCATION:</strong> {{this.client_location}}</p>
          <p><strong>CONTACT:</strong> {{this.client_contact}}</p>
          <p><strong>SERVICE TYPE:</strong> {{this.services}}</p>
          <p><strong>DESCRIPTION:</strong> {{this.specific_service}}</p>
        </div>
        <div class="col-xs-6 accept-form">
          <form method="POST" action="/api/update" class="button-size">
            <input input type="hidden" type="text" name="job_id" value={{this.id}}><br>
            <button type="submit" class="btn btn-default">Accept</button>
          </form>
          <form method="POST" action="/api/delete" class="button-size">
            <input input type="hidden" type="text" name="job_id" value={{this.id}}><br>
            <button type="submit" class="btn btn-default">Reject</button>
          </form>
        </div>
      </div>
    </div>
  </div>
  {{/each}}
</div>